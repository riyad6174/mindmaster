import AdminInProgress from '@/components/Admin/AdminInProgress';

export default function AdminAddProgramPage() {
  return (
    <AdminInProgress 
      title="Add New Program" 
      subtitle="Create and launch new educational modules, specify schedules, and set enrollment guidelines."
    />
  );
}
