import AdminInProgress from '@/components/Admin/AdminInProgress';

export default function AdminCalendarPage() {
  return (
    <AdminInProgress 
      title="Academic Calendar" 
      subtitle="Edit term sessions, holidays, and event schedules for students and parents to view on the website."
    />
  );
}
