import AdminInProgress from '@/components/Admin/AdminInProgress';

export default function AdminNoticesPage() {
  return (
    <AdminInProgress 
      title="Manage Notices" 
      subtitle="Publish announcements, center closures, or key school updates for parents and students."
    />
  );
}
